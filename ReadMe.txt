python 版本 3.10

安装ffmpeg
命令：brew install ffmpeg


创建虚拟环境目录
source .venv/bin/activate 激活虚拟环境
使用pip3 install -r requirements.txt 安装依赖

1. 语言转文字，使用高精度的 Whisper
   本地模型安装命令：
    pip install whisper
2. 文字转语言，使用高质量的 Google TTS
